namespace Alarms.Web.Helper;
public class SortHelper/*<T>*/
{
    //public static SortMode<T> GetSort(string defaultField, string sortfield = "", string sortorder = "")
    //{
    //    var order = sortorder == "asc" ? SortDirection.Ascending : SortDirection.Descending;

    //    return !string.IsNullOrWhiteSpace(sortfield)
    //        ? SortMode<T>.Create(sortfield, order)
    //        : SortMode<T>.Create(defaultField, order);
    //}
}

